// a função gets é implementada dentro do sistema para ler as entradas(inputs) dos dados
let totalDeCasos = parseInt(gets());
let msgs = [];
let qtdEntradas = 0;
let numReordem = 0;
let qtdSms;

for (let i = 0; i < totalDeCasos; i++) {

    qtdEntradas = parseInt(gets());
    qtdSms = gets();
    msgs[qtdEntradas];

    
}